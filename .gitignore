.env
.venv/
__pycache__/
*.pyc
node_modules/
bench/r3-tts/compare/**/*.wav
bench/r3-tts/round2/**/*.wav
# История прогонов: отчёт дописывается сюда одной строкой на каждый finish.
# Это рантайм-артефакт, а не исходник, и растёт он быстро.
data/reports/

# Побочный файл: имя базы «:memory:» уехало на диск как путь.
:memory:*
