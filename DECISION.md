# DECISION — рекомендованный стек

Дата: 2026-09-03. Железо разработки и, по умолчанию, демо: MacBook Air M4,
16 ГБ, macOS 26.6.2. Все числа измерены на нём, сырьё в `bench/results/`.

Формат: на компонент — выбор, альтернатива, обоснование числом и что нужно,
чтобы переключиться за час.

---

## 1. STT — GigaAM-v3 CTC int8 ONNX через sherpa-onnx

**Выбор:** `csukuangfj/sherpa-onnx-nemo-ctc-giga-am-v3-russian-2025-12-16`,
чанк 480 мс, хоп 0.5 с. Лицензия **MIT**.

**Альтернатива:** её нет. Parakeet TDT 0.6B v3 был кандидатом в фолбэки по
синтетике, но **выбыл на живой речи** — см. ниже.

**Обоснование числом (R1, 35 клипов на конфигурацию):**

| | GigaAM | Parakeet | Whisper turbo |
|---|---|---|---|
| финализация после решения эндпоинтера | **195 мс** | 438 мс | 25400 мс |
| worst RTF | **0.38** | 0.94 | 19.73 |
| отставание от реального времени | **82 мс** | 390 мс | 23964 мс |
| первый партиал | 1038 мс | **83 мс** | 81 мс |
| медиана одного декода | **88 мс** | 317 мс | 1434 мс |

GigaAM — единственный с честным запасом по реальному времени. Parakeet
выигрывает первый партиал в 12 раз за счёт настоящей инкрементальности
(ротирующийся KV-кэш на слой энкодера, не пересчёт буфера), но `RTF 0.94`
означает отсутствие запаса: под нагрузкой демо он начнёт отставать.

Whisper large-v3-turbo в потоке на M4 непригоден **принципиально**: замер
стоимости декода против длины буфера (771 точка) показал, что она постоянна
~1.25–1.5 с независимо от длины, потому что Whisper паддит вход до окна 30 с.
Обрезка окна не помогает. Минимальный хоп для реального времени — 1.5 с.

**Почему Parakeet выбыл (живая речь, 35 записей):** его первый партиал —
**мусор в 32 клипах из 35**, почти всегда английское «Yeah.», выдуманное на
ведущей тишине. У GigaAM — 0 из 35. То есть преимущество «партиал за 83 мс
против 1038», ради которого он и держался в фолбэках, оказалось галлюцинацией:
для спекулятивного prefill такой партиал хуже, чем никакого — запрос уйдёт с
промптом «Yeah.» и его придётся отменять. Плюс `worst RTF` на живой речи
вырос до **1.09**, за предел реального времени.

**Переключение за час:** движок остаётся в `REGISTRY["parakeet"]` и померен,
но брать его теперь незачем.

**Английские термины — измерено на живой речи, риск снят частично.**
GigaAM не ломает термины, а транслитерирует их, обычно узнаваемо:
`system design` → «систем дизайн», `Python` → «пайтен», `k8s` → «кубернетес»,
`backlog` → «бэклог», `N+1` → «энпласс ван». По-настоящему ломаются только
побуквенные аббревиатуры (`CI/CD` → «сиасизи») и имена, слипающиеся с
предлогом (`на GitLab` → «нагетлаб»).

**Следствие для реализации:** сопоставлять сценарий с транскриптом по точному
совпадению латиницы **нельзя** — латиница не возвращается никогда. Матчинг
должен быть семантическим через LLM. Это архитектурное требование, а не
настройка.

---

## 2. Определение конца хода — Silero VAD с порогом тишины 1000 мс

**Выбор:** `silero-vad` (MIT), порог тишины **1000 мс**.

**Альтернатива:** энергетический детектор с тем же порогом 1000 мс.

**Обоснование числом — живая речь с настоящими заминками, 20 фраз с 31
заминкой плюс 5 терминальных контрольных:**

| детектор | фраз испорчено | задержка на терминальных | пропущено терминальных |
|---|---|---|---|
| energy 1500 мс | 0/20 (0%) | 1504 мс | 1/5 |
| **silero-vad 1000 мс** | **1/20 (5%)** | **1256 мс** | **0/5** |
| energy 1000 мс | 2/20 (10%) | 1000 мс | 0/5 |
| smart-turn 0.97 | 7/20 (35%) | 192 мс | **3/5** |
| energy 700 мс | 8/20 (40%) | 680 мс | 0/5 |
| smart-turn 0.5 / 0.8 | 15/20 (75%) | 256 мс | 1/5 |

**smart-turn v3.2 из рекомендации убран, и это разворот относительно вывода
по синтетике.** Там он выглядел лучшим кандидатом (256 мс на законченной
реплике), и я предполагал, что синтетические паузы его занижают. На живой речи
вышло наоборот: он уверенно принимает **тянущийся гласный** за конец хода —
на «зависит от-о-о…» выдаёт p = 0.973 посреди слова и обрывает человека.
Проверено на трёх клипах поимённо, это не артефакт гейта. Плюс при пороге 0.97
он молчит на 3 терминальных фразах из 5.

Вероятная причина: живая русская заминка — это растяжка гласного, а не пауза,
и в обучающем наборе модели таких примеров, судя по поведению, мало.

**Переключение на альтернативу за час:** оба детектора в
`bench/r2-endpoint/detectors.py`, меняется строка спецификации. Разница между
silero-1000 и energy-1000 — одна фраза на выборке в двадцать, то есть в пределах
шума; energy проще и не тянет зависимость.

**Обязательное дополнение — двухфазный barge-in** (приём из Rula, MIT):
сначала быстро приглушить воспроизведение, потом дать STT подтвердить.
При 5% ложных срезов это уже не спасение конструкции, а вежливость.

**Чего не берём — 1: Deepgram Flux** (ключ был, померен 2026-09-03).
На пороге по умолчанию 0.7 портит **13 фраз из 20** при задержке 760 мс;
на пороге 0.8 и выше уверенность никогда не достигает порога — ноль ложных
срезов и ноль обнаруженных концов хода, включая все 5 терминальных фраз.
Рабочей точки нет. Как STT: первый партиал 517 мс, EndOfTurn с транскриптом
760 мс — выигрыша против локального GigaAM не даёт, а сеть добавляет.

**Чего не берём — 2:** `livekit/turn-detector`. Лицензия запрещает использование
вне фреймворка LiveKit Agents — «cannot use the LiveKit models on a standalone
basis or with any other frameworks». Померить его отдельным скриптом нельзя
даже юридически.

---

## 3. TTS — Silero v4_ru

**Выбор:** `silero_tts`, точка входа `speaker="v4_ru"`, голос выбирается на
слух из пяти (`aidar, baya, kseniya, xenia, eugene`). Лицензия **MIT**.

**Альтернатива:** ElevenLabs Flash v2.5.

**Обоснование числом (R3):** TTFB первой клаузы **медиана 9 мс, максимум
13 мс**; RTF **0.008–0.013**, то есть примерно стократ быстрее реального
времени. Вендор Silero заявляет RTF 0.04 — измеренное в 3–5 раз лучше
заявленного.

Изначально Silero планировался офлайн-фолбэком. По измерениям он лучше
подходит на роль **основного**: TTS перестаёт быть статьёй бюджета вообще
(9 мс из 3000), нативно русские голоса, ноль зависимости от сети и биллинга.

**Обязательное условие: прогрев двумя фразами при старте сервиса.** Измерено:
первые **два** вызова стоят ~520 мс каждый, один прогревочный вызов не
помогает. Без этого полсекунды достанутся первой реплике агента.

**ElevenLabs померен (ключ появился по ходу), и он проигрывает по задержке
в 175 раз:** TTFB **1605 мс** медианы против 9 мс у Silero. Проверено, что это
не артефакт моего клиента — с прогретым переиспользуемым TCP/TLS-соединением
1588 мс. Заявленные ~75 мс относятся ко времени инференса, а не к TTFB у
клиента. Разброс огромный: один прогон из пяти дал 207 мс, остальные
1556–2455 мс. Замер из одной географической точки, один сеанс, ~15 запросов.

Alignment у ElevenLabs есть и рабочий (5/5, шаг 58 мс), но локальное
выравнивание через GigaAM даёт 40 мс и не требует сети.

**Причина переключиться остаётся одна:** если Silero на слух окажется
неприемлемым. Тогда бюджет придётся пересобирать вокруг 1.6 с и почти наверняка
вводить бэкчэннел. **Оценку на слух я сделать не могу** — сэмплы в
`bench/r3-tts/samples/`, слушать должен человек.

**Не проверено:** Qwen3-TTS 12Hz 1.7B. Стримингового кода в официальном
репозитории нет; форки существуют (`dffdeeq`, `kunzite-app`, Rust-порт
`TrevorS/qwen3-tts-rs` с Metal). Не запускался: 1.7B на M4 ради тембров, про
которые заранее известно, что нативно русских среди них нет — плохой размен
на двухдневном хакатоне при наличии Silero с RTF 0.01.

---

## 4. Липсинк — висемы из посимвольных таймингов (уровень 2)

**Выбор:** синтезируем фразу целиком (Silero, RTF 0.01), выравниваем тем же
GigaAM, получаем посимвольные таймкоды, раскладываем в 13 висем.

**Альтернатива:** `WebAudio AnalyserNode` → `jawOpen` (уровень 3).

**Обоснование числом (R4):** накладные расходы **медиана 97 мс на фразу**
(максимум 116 мс), худший RTF 0.023, шаг таймкодов **40 мс** — точнее, чем
кадр анимации на 30 FPS, и в пять раз точнее допуска 200 мс.

Главное свойство: **никакой зависимости от ElevenLabs, сети и GPU.** Один и
тот же распознаватель работает и на входе, и на выравнивании выхода — меньше
движущихся частей.

**Переключение за час:** уровень 3 уже реализован в `spikes/s2-timeline`,
переключатель источника мимики. Измерено: отставание рта от аудио **5 мс**
при корреляции 0.94. Суммарный рассинхрон уровня 3 — до 46 мс, вчетверо
внутри допуска. Переключаться, если выравнивание где-то развалится.

**Чего не берём: NVIDIA Audio2Face-3D.** Требует CUDA 12.8+, TensorRT 10.13+,
драйвер R570+; CPU- или не-NVIDIA варианта не существует. На M4 не запускается
принципиально. Проверить можно только на арендованном GPU (~$1.70), но если
демо идёт с макбука, а A2F — на Runpod, в бюджет добавляется сетевой RTT и
жёсткая зависимость от интернета на площадке. Уровень 2 при этом уже работает
с четырёхкратным запасом. **Не измерялся сознательно, а не по недосмотру.**

---

## 5. Транспорт аудио и мимики — один PTS, якорь на генерацию

**Выбор:** два трека с общим `pts_ms` от начала генерации; аудио планируется
от одного якоря `t0` на всю генерацию; рендер-луп на каждом кадре берёт
`AudioContext.currentTime` и интерполирует между соседними кадрами
блендшейпов. Никаких `setInterval`.

**Обоснование числом (S2, 285 кадров):** дрейф p50 **0 мс** по построению,
`rAF` 16.7 мс, `outputLatency` **24 мс**, подвисаний 9 из 285 (~3%).

Два практических следствия из замера:
- **`outputLatency` надо вычитать.** Иначе рот опережает звук на 24 мс,
  потому что рендер живёт по часам графа, а ухо слышит позже.
- **Кадры лица должны приходить с опережением аудио.** Подвисания — это
  моменты, когда рендер вышел за пределы имеющихся кадров и держал последний.

---

## 6. Отмена — generation_id + сброс буфера + fade 25 мс

**Выбор:** при перебивании `generation_id` инкрементируется; сервер гасит
LLM и TTS; клиент **безусловно** дропает всё с чужим `generation_id`;
воспроизведение останавливается сбросом буфера с fade 25 мс.

**Обоснование числом (S3):**

| способ | остановка | + выход устройства |
|---|---|---|
| перестать подавать в плеер | **6960 мс** | 6984 мс |
| сброс буфера, жёсткий стоп | 0 мс | 24 мс |
| **сброс буфера + fade 25 мс** | 23 мс | **47 мс** |

Серверная отмена — 0.1–7.2 мс, узким местом не является. **Вся задержка
сидит в клиентском аудиобуфере**: на момент отмены там было накоплено ~6.9 с.
Подозрение из постановки задачи подтвердилось буквально. Останавливать надо
выход, а не подачу.

Fade стоит 23 мс сверх жёсткого стопа при пороге 300 мс — берём без раздумий.

---

## 7. Аватар — GLB с 52 ARKit-морфами, источник Avaturn

**Выбор:** Avaturn (бесплатный тариф достаточен для демо, но некоммерческий;
Pro $19/мес снимает ограничение).

**Альтернатива:** Avatar SDK MetaPerson; в крайнем случае любая game-ready
голова с одним морфом открытия рта.

**Обоснование:** Ready Player Me закрылся 31 января 2026 после покупки
Netflix (объявлена 19 декабря 2025) — креатор и API отключены, большинство
туториалов нерабочие. Проверка кандидата автоматизирована:
`bench/r5-avatar/inspect_glb.py` даёт вердикт по 52 ARKit-морфам, весу и
ловушкам (VRM под видом GLB, потерянные имена морф-таргетов).

**VRM не берём:** пять пресетов рта (`aa ih ou ee oh`) для 13 висем мало.
Спека формально разрешает `expressions.custom`, но типовые VRM-модели других
морфов рта не содержат — ограничение в моделях, а не в формате. Плюс жанр:
VRM аниме, а нужен полуреалистичный персонаж.

**Переключение за час:** если 52 морфов не окажется, переходим на уровень 3
липсинка, которому хватает **одного** морфа открытия рта.

---

## 8. Фреймворк — свой конвейер, не чужой

**Выбор:** собственный конвейер на ~300 строк. Язык — **Python** (вы дали
свободу выбора): все SDK питоновские, GigaAM/Silero/smart-turn запускаются
из него напрямую, референсы читаемы.

**Обоснование:** все три кандидата тянут неприемлемую нагрузку.
LiveKit Agents — лицензионное ограничение на веса детектора. Pipecat —
чужая модель процессоров ради двух дней. Rula — Windows + WSL2 + CUDA +
64–128 ГБ RAM, при 2 коммитах и пометке автора «not production-ready».

**Из чужого берём ровно три вещи, все под пермиссивной лицензией:**
веса smart-turn-v3 (BSD-2), схему `generation_id` (идея из Rula, MIT),
двухфазный barge-in (идея из Rula, MIT).

---

## 9. LLM — DeepSeek, но только вместе со спекуляцией с перезапуском

**Выбор:** DeepSeek `deepseek-chat` (ключ предоставлен), **обязательно** со
спекулятивной генерацией с перезапуском по мере роста транскрипта.

**Альтернатива:** любая модель с меньшим TTFT; в крайнем случае локальная
модель на M4 (не проверена, см. RISKS).

**Обоснование числом (S1, реальный API):**

| режим | n | prefill попал | TTFT медиана | итог медиана | **в пороге 3000 мс** |
|---|---|---|---|---|---|
| без спекуляции | 8 | 0/8 | 2074 мс | 4027 мс | **1 / 8** |
| спекуляция, один запуск prefill | 20 | 0/20 | 1926 мс | 3807 мс | **5 / 20** |
| **спекуляция с перезапуском** | 20 | **12/20** | — | **2730 мс** | **13 / 20** |

Когда prefill попадает (12 из 20): TTFT **484 мс**, итог 2426 мс, в пороге
**10 из 12**. Когда не попадает: TTFT 2018 мс, итог 3448 мс, в пороге 3 из 8.

**Это единственный компонент, где выбор не является выбором: без спекуляции
целевая метрика не выполняется.** TTFT 1899 мс медианы (разброс 1772–2543)
против моей первоначальной оценки 400–800 мс — ошибка втрое.

**Ключевая деталь реализации, без которой спекуляция бесполезна.** Первая
версия запускала prefill один раз, на 12 символах транскрипта. Результат —
**0 попаданий из 20**: к моменту эндпоинта ранний промпт покрывал треть
финальной фразы (медиана покрытия 0.35), и запрос приходилось выбрасывать.
Рабочая версия **перезапускает запрос при росте транскрипта на 40%**, держа в
полёте самый свежий вариант: покрытие 0.98, попадания 3 из 5, медиана 2
перезапуска на реплику. Цена — 2–3 запроса вместо одного.

**Обязательное дополнение — бэкчэннел.** Короткое «угу», синтезированное
заранее и лежащее готовым PCM, уходит в динамик через **131 мс** после решения
эндпоинтера. Оцениваемая метрика «первый звук» тогда выполняется **53 раза из
53**: 517 мс медианы на быстром пути, 1809 мс на безопасном. Без бэкчэннела —
21 из 53. Это самый дешёвый ход в проекте: полдня работы, ничего не ломает.

Он **не ускоряет ответ** — содержательная реплика по-прежнему на 2700–3600 мс.
Если жюри мерит время до осмысленного ответа, а не до первого звука, приём не
поможет, и тогда нужен более быстрый провайдер.

**Переключение за час:** адаптер провайдера — это класс с методами `start()`
и `cancel()` в `spikes/s1-latency/pipeline.py`, около 30 строк. Смена
провайдера — одна строка в `config.json`.

**Оговорка, которую нельзя замалчивать: даже с работающей спекуляцией
метрика выполняется в 13 случаях из 20, а не всегда.** Оставшиеся промахи —
это реплики, где prefill не успел попасть: короткие реплики не дают
спекуляции времени на перезапуск, а на них же приходится быстрый путь
эндпоинтера, так что итог всё равно ниже. Совсем плохие случаи — длинные
реплики с сильно меняющимся хвостом транскрипта.

---

## Сводка стека

| компонент | выбор | лицензия | ключевое число |
|---|---|---|---|
| STT | GigaAM-v3 CTC int8 ONNX | MIT | живая речь: WER 0.120, RTF 0.42 |
| конец хода | silero-vad, порог тишины 1000 мс | MIT | живая речь: 5% ложных срезов, 1256 мс |
| TTS | Silero v4_ru | MIT | TTFB 9 мс, RTF 0.01 |
| липсинк | висемы из таймингов GigaAM | MIT | 97 мс/фразу, шаг 40 мс |
| синхронизация | общий PTS + якорь + интерполяция | — | дрейф 0 мс, до 46 мс к уху |
| отмена | generation_id + flush + fade 25 мс | — | 47 мс |
| аватар | Avaturn GLB, 52 ARKit | free/$19 | проверяется скриптом |
| бэкенд | Python, свой конвейер | — | — |
| LLM | DeepSeek + спекуляция + бэкчэннел | — | первый звук **53/53**; ответ 13/20 |

**Весь рекомендованный стек — офлайновый и работает без интернета,
кроме LLM.** Это не было целью; так получилось по измерениям. Практическое
следствие: неизвестность про интернет на площадке бьёт ровно по одному
компоненту, и именно его надо страховать.
