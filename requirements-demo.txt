# Python 3.11+; версии намеренно заданы диапазонами, чтобы работали колёса
# под macOS и Linux. Проверено локально на Python 3.12.
huggingface-hub>=0.25,<2
httpx>=0.27,<1
numpy>=1.26,<3
# Нужен загрузчику Silero через torch.hub. В рабочем окружении приезжал
# транзитивно, и на чистой установке по инструкции всё падало с
# ModuleNotFoundError — поймано прогоном README с нуля.
omegaconf>=2.3,<3
sherpa-onnx>=1.10,<2
torch>=2.2,<3
